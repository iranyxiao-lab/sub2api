# 智力测试发布记录（2026-09-25）

## 交付

- 功能提交 `c04642c46`，分阶段发布控制 `c6aee749e`，运行镜像源码提交 `e671b4dd5`；均已推送 fork。
- 镜像 `sub2api:intelligence-e671b4dd5`，镜像 ID `sha256:13f87f1224711d20927a6cc38e6593fe1d3442b9727e3c87c904a2901340d0a7`。
- 目标为 SSH 别名 homestay 的 `/opt/sub2api`。持久化应用服务 `sub2api-next`，端口 `127.0.0.1:18083`；旧应用停止保留，数据库与 Redis 未重建。
- Compose 校验和 `b87b4436841b4adf243c8b4ba945f793373206e4dc5a6e217af3334fee44fdb4`；代理配置校验和 `c90e9fbd3552c02f6156d1743712e0d7bb0f78dc43c62c907a981474e7624b1d`。
- 备份与现场证据：服务器 `/opt/sub2api/releases/intelligence-e671b4dd5/`，包括自定义格式数据库备份（`pg_restore --list` 已验证）、旧 Compose、旧代理配置、激活时间及观察摘要；原迁移前 SQL 备份在 `/opt/sub2api/backups/sub2api.20260925-005008.sql`。

## 验证范围

- 定向后端单测通过，包含 31 秒真实流解析、浏览器取消后后台继续、部分回答保留、UTF-8 截断。真实 PostgreSQL 集成测试通过并发入队、幂等、租约恢复、历史分页及定时任务去重。
- 发布门控 4 项测试通过，覆盖默认即时启动、延迟启动、标记激活、关闭后不再启动。Windows 的 race 检查因 CGO 未启用未执行。
- 前端 15 项测试、lint、类型检查与镜像生产构建通过。真实 Chromium 使用本地模拟数据验证历史内联展示、320px/240px 尺寸、无横向溢出、关闭放大预览、立即显示排队记录和打开面板不调用模型；这不是生产浏览器验收。
- 全量单测并非全绿：`TestOllamaProbeCallback_StaleLongDoesNotOverrideNewShort` 断言失败；3 个 `TestPgDumper*` 用例因 Windows 缺少 `sh` 失败，相关实现未在本次功能修改中变更。未以这些失败为由删除或放宽测试。
- 生产管理员历史接口验证通过。受控运行 #7 在 12ms 内返回 202/queued，重复请求返回同一 ID；执行约 97.8 秒后终态为 failed/upstream_error，错误为 `Upstream request failed`，没有 `context canceled`。这证明任务不再受 30 秒浏览器等待限制，但不证明该账号的模型回答成功；没有自动重试。

## 发布异常与限制

- 首次候选实例未正确隔离周期任务，发现后停止，未将代理流量切给它；随后补齐门控并用新镜像重做候选验证。该实例已运行过后台启动逻辑，不能据此宣称完全没有副作用。
- 正式切换先通过代理语法检查和平滑 reload，再确认旧 worker 消失、18082 已连接数为零、无执行中测试，最后停止旧应用并激活新周期任务（北京时间 01:16:35）。
- 观察期间主机在 01:36:26 整机重启，Docker 于 01:36:37 启动，多业务容器于约 01:36:45 自动恢复；未执行过主机或 Docker 重启命令。所查日志未见 OOM，但原因尚未确认，不能宣称整个观察窗口零中断。
- 重启后新应用、数据库、Redis 及其他业务容器恢复健康，旧应用仍为停止状态。部署门控不支持已启用的链上运行时；该情况下启动会明确拒绝分阶段模式。
